#include<iostream>
#include<conio.h>
#include "stack.h"
#include<fstream>

using namespace std;

char readfile() {
     
                    ifstream _readfile("inputfile.txt")
                    
                }

int main() {
Stack<int> S;
/*
S.Push(5);
S.Push(2);
S.Push(1);
cout<<S.Pop();
cout<<S.Peek();
if(S.IsEmpty())
cout<<S.Pop();
cout<<S.Peek();
*/

getch();
return 0;
}
